<UserControl
    x:Class="LighthouseMonitoring.View.MonitoringView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LighthouseMonitoring.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:LighthouseMonitoring.ViewModel"
    d:Background="White"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="Coral">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Monitoring" />
        <TextBlock Text="{Binding MonitoringSystem.LastPolled, StringFormat='{}{0:s}'}" Grid.Row="1"/>
        <ItemsControl Grid.Row="2" ItemsSource="{Binding MonitoringSystem.Lathes}" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!--<uc:LatheState Lathe="{Binding}"/>-->
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--<Style x:Key="Default" TargetType="Item">
        <Setter Property="Background" Value="{StaticResource materialBackground}" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="{StaticResource materialOffBlack}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border Background="{TemplateBinding Background}" CornerRadius="10">
                        <ContentPresenter Margin="5,2" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    -->
    <!--<Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource materialSelectedColour}" />
                <Setter Property="BorderThickness" Value="2" />
            </Trigger>
        </Style.Triggers>-->
    <!--
    </Style>

    -->
    <!--  Don't touch  -->
    <!--
    <Style BasedOn="{StaticResource Default}" TargetType="ItemsPresenter" />-->
</ResourceDictionary>
<Window
    x:Class="ProjectLighthouse.View.EditLMOWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="clr-namespace:ProjectLighthouse.View.UserControls"
    Title="Edit Order"
    Width="600"
    ResizeMode="NoResize"
    SizeToContent="Height"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">


    <Grid Margin="25">
        <!--  Header Area  -->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Name="nameText"
                Style="{StaticResource title}"
                Text="M00001" />
            <Button
                Grid.Column="2"
                Click="closeButton_Click"
                Content="Close"
                Style="{StaticResource CancelButton}" />
            <Separator
                Grid.Row="1"
                Grid.ColumnSpan="3"
                Margin="0,0,0,15" />
        </Grid>
        <!--  Main controls  -->
        <StackPanel Grid.Row="1">
            <TextBlock Style="{StaticResource section}" Text="Administration" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid>
                    <TextBox
                        x:Name="PORef"
                        Style="{StaticResource UsernameTextBox}"
                        TextChanged="PORef_TextChanged" />
                    <TextBlock
                        x:Name="poRefGhostText"
                        Margin="35,0"
                        VerticalAlignment="Center"
                        FontSize="18"
                        FontStyle="Italic"
                        Foreground="#aaaaaa"
                        IsHitTestVisible="False"
                        Text="POR..." />
                </Grid>
                <StackPanel Grid.Row="1" Margin="10">
                    <CheckBox
                        x:Name="program"
                        Click="program_Click"
                        Content="Has Program" />
                    <CheckBox
                        x:Name="ready"
                        Click="ready_Click"
                        Content="Ready to Set" />
                </StackPanel>

                <StackPanel
                    Grid.RowSpan="2"
                    Grid.Column="1"
                    Margin="30,0"
                    VerticalAlignment="Center">


                    <TextBlock
                        Margin="0,5,0,3"
                        Style="{StaticResource section}"
                        Text="Set Status" />

                    <RadioButton
                        x:Name="not_started_radio"
                        Checked="not_started_radio_Checked"
                        Content="Not Started" />
                    <RadioButton
                        x:Name="running_radio"
                        Checked="running_radio_Checked"
                        Content="Running" />
                    <RadioButton
                        x:Name="complete_radio"
                        Checked="complete_radio_Checked"
                        Content="Complete" />
                </StackPanel>
            </Grid>

            <!--  Items List  -->
            <TextBlock Style="{StaticResource section}" Text="Items" />
            <ListView x:Name="ItemsListBox">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <uc:DisplayLMOItems LatheManufactureOrderItem="{Binding}" MouseDoubleClick="DisplayLMOItems_MouseDoubleClick" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <TextBlock Style="{StaticResource section}" Text="Notes" />
            <ItemsControl
                x:Name="NotesDisplay"
                Grid.Row="1"
                MinHeight="50"
                MaxHeight="200"
                HorizontalContentAlignment="Stretch"
                ScrollViewer.CanContentScroll="False">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <uc:DisplayNote Note="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Grid.Row="2" Margin="5">
                <Border Background="{StaticResource materialSurface}" CornerRadius="10" />
                <Grid Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        x:Name="Message"
                        VerticalContentAlignment="Center"
                        Background="Transparent"
                        TextWrapping="WrapWithOverflow"
                        BorderThickness="0"
                        Foreground="{StaticResource materialOffBlack}"
                        TextChanged="Message_TextChanged" 
                        KeyDown="Message_KeyDown"/>
                    <TextBlock
                        x:Name="ComposeGhost"
                        Margin="10,0"
                        VerticalAlignment="Center"
                        FontStyle="Italic"
                        Foreground="#aaaaaa"
                        IsHitTestVisible="False"
                        Text="Compose message..." />
                    <Button
                        x:Name="SendButton"
                        Grid.Column="1"
                        Margin="10,0,0,0"
                        Click="SendButton_Click"
                        Style="{StaticResource SendButton}" />
                </Grid>
            </Grid>
        </Grid>
        <Button
            x:Name="saveButton"
            Grid.Row="3"
            Width="150"
            HorizontalAlignment="Right"
            Click="saveButton_Click"
            Content="Save and Close" />
    </Grid>
</Window>

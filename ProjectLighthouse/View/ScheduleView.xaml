<UserControl x:Class="ProjectLighthouse.View.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectLighthouse.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <UserControl.Resources>
        <Style x:Key="headerTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="/Resources/Font/#Proxima Nova"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource colNeutral}"/>
        </Style>
    </UserControl.Resources>
    
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Unallocated" Grid.Column="0" Grid.Row="0" Style="{StaticResource headerTextStyle}"/>
        <ListBox Grid.Column="0" Grid.Row="1"></ListBox>

        <TextBlock Text="Citizen One" Grid.Column="2" Grid.Row="0" Style="{StaticResource headerTextStyle}"/>
        <ListBox Grid.Column="2" Grid.Row="1"></ListBox>

        <TextBlock Text="Citizen Two" Grid.Column="4" Grid.Row="0" Style="{StaticResource headerTextStyle}"/>
        <ListBox Grid.Column="4" Grid.Row="1"></ListBox>
        
    </Grid>
</UserControl>

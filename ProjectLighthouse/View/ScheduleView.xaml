<UserControl x:Class="ProjectLighthouse.View.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectLighthouse.View"
             xmlns:uc="clr-namespace:ProjectLighthouse.View.UserControls"
             xmlns:vm="clr-namespace:ProjectLighthouse.ViewModel"
             xmlns:converters="clr-namespace:ProjectLighthouse.ViewModel.ValueConverters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    
    <UserControl.Resources>
        <vm:ScheduleViewModel x:Key="vm"/>
        <Style x:Key="headerTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="/Resources/Font/#Proxima Nova"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource colNeutral}"/>
        </Style>
        <converters:intToDays x:Key="intToDayConverter"/>
    </UserControl.Resources>
    
    
    <Grid DataContext="{StaticResource vm}">
        <Grid>
            <TabControl SelectedValue="{Binding SelectedTab}" Loaded="TabControl_Loaded">

                <TabItem IsEnabled="False" Visibility="Collapsed">
                    <TabItem.Header>
                        <TextBlock Text="Overview"/>
                    </TabItem.Header>
                    <TextBlock Text="Coming Soon" FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                </TabItem>

                <TabItem IsSelected="True" Tag="">
                    <TabItem.Header>
                        <TextBlock Text="Unallocated"/>
                    </TabItem.Header>
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Unallocated Manufacture Orders" FontSize="30"/>

                        <ListView HorizontalContentAlignment="Stretch" 
                                  ItemsSource="{Binding CompleteOrders, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                  ScrollViewer.CanContentScroll="False" 
                                  ScrollViewer.VerticalScrollBarVisibility="Auto" 
                                  Grid.Row="1"
                                  Margin="0,10"
                                  BorderBrush="DodgerBlue">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <uc:DisplayLMOScheduling orderObject="{Binding}" MouseDown="DisplayLMOScheduling_MouseClick"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <StatusBar Background="Transparent" Grid.Row="2">
                            <Grid>
                                <Rectangle Height="28" Fill="{StaticResource materialSecondary}" RadiusX="14" RadiusY="14">
                                    <Rectangle.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="20" Direction="220" Color="Black" Opacity="0.3"/>
                                    </Rectangle.Effect>
                                </Rectangle>
                                <TextBlock Foreground="{StaticResource materialOnSecondary}" FontSize="16" VerticalAlignment="Center"
                                           Padding="20,3,20,5">
                                        <Run Text="{Binding filteredOrders.Count, Mode=OneWay, StringFormat='{}{0} Orders'}"/>
                                        <Run Text="//"/>
                                        
                                        <Run Text="Workload:"/>
                                        <Run Text="{Binding totalTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, Converter={StaticResource intToDayConverter}}"/>
                                        <Run Text="days"/>
                                </TextBlock>
                            </Grid>
                        </StatusBar>
                    </Grid>
                </TabItem>

                <TabItem Tag="C01">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Citizen 1" VerticalAlignment="Center"/>
                            <!--<Grid Margin="10,1,0,1">
                                <Rectangle Fill="LightSeaGreen" RadiusX="10" RadiusY="10"/>
                                <TextBlock Text="L20" Padding="10,1" Foreground="White" FontWeight="Bold"/>
                            </Grid>-->
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Citizen One Schedule" FontSize="30"/>
                        <ListView HorizontalContentAlignment="Stretch" 
                                  ItemsSource="{Binding CompleteOrders, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                  ScrollViewer.CanContentScroll="False" 
                                  ScrollViewer.VerticalScrollBarVisibility="Auto" 
                                  Grid.Row="1"
                                  Margin="0,10"
                                  BorderBrush="DodgerBlue">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <uc:DisplayLMOScheduling orderObject="{Binding}" MouseDown="DisplayLMOScheduling_MouseClick"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <StatusBar Background="Transparent" Grid.Row="2">
                            <Grid>
                                <Rectangle Height="28" Fill="{StaticResource materialSecondary}" RadiusX="14" RadiusY="14">
                                    <Rectangle.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="20" Direction="220" Color="Black" Opacity="0.3"/>
                                    </Rectangle.Effect>
                                </Rectangle>
                                <TextBlock Foreground="{StaticResource materialOnSecondary}" 
                                           FontSize="16" 
                                           VerticalAlignment="Center"
                                           Padding="20,3,20,5">
                                    <Run Text="{Binding filteredOrders.Count, Mode=OneWay, StringFormat='{}{0} Orders'}"/>
                                    <Run Text="//"/>
                                        
                                    <Run Text="Workload:"/>
                                    <Run Text="{Binding totalTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, Converter={StaticResource intToDayConverter}}"/>
                                    <Run Text="days"/>
                                        
                                    <Run Text="//"/>
                                    <Run Text="Booked up to:"/>
                                    <Run Text="{Binding bookedTo, StringFormat='{}{0:dd MMMM}', UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                                </TextBlock>
                            </Grid>
                        </StatusBar>
                    </Grid>
                </TabItem>

                <TabItem Tag="C02">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Citizen 2" VerticalAlignment="Center"/>
                            <!--<Grid Margin="10,1,0,1">
                                <Rectangle Fill="LightSeaGreen" RadiusX="10" RadiusY="10"/>
                                <TextBlock Text="L20" Padding="10,1" Foreground="White" FontWeight="Bold"/>
                            </Grid>-->
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Citizen Two Schedule" FontSize="30"/>
                        <ListView HorizontalContentAlignment="Stretch" 
                                  ItemsSource="{Binding CompleteOrders, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                  ScrollViewer.CanContentScroll="False" 
                                  ScrollViewer.VerticalScrollBarVisibility="Auto" 
                                  Grid.Row="1"
                                  Margin="0,10"
                                  BorderBrush="DodgerBlue">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <uc:DisplayLMOScheduling orderObject="{Binding}" MouseDown="DisplayLMOScheduling_MouseClick"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <StatusBar Background="Transparent" Grid.Row="2">
                            <Grid>
                                <Rectangle Height="28" Fill="{StaticResource materialSecondary}" RadiusX="14" RadiusY="14">
                                    <Rectangle.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="20" Direction="220" Color="Black" Opacity="0.3"/>
                                    </Rectangle.Effect>
                                </Rectangle>

                                <TextBlock Foreground="{StaticResource materialOnSecondary}" 
                                           FontSize="16" 
                                           VerticalAlignment="Center"
                                           Padding="20,3,20,5">
                                    <Run Text="{Binding filteredOrders.Count, Mode=OneWay, StringFormat='{}{0} Orders'}"/>
                                    <Run Text="//"/>
                                        
                                    <Run Text="Workload:"/>
                                    <Run Text="{Binding totalTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, Converter={StaticResource intToDayConverter}}"/>
                                    <Run Text="days"/>
                                        
                                    <Run Text="//"/>
                                    <Run Text="Booked up to:"/>
                                    <Run Text="{Binding bookedTo, StringFormat='{}{0:dd MMMM}', UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                                </TextBlock>
                            </Grid>
                        </StatusBar>
                    </Grid>
                </TabItem>

                <TabItem Visibility="Hidden" Tag="C03">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Citizen 3" VerticalAlignment="Center"/>
                            <Grid Margin="10,1,0,1">
                                <Rectangle Fill="LightSeaGreen" RadiusX="10" RadiusY="10"/>
                                <TextBlock Text="L20" Padding="10,1" Foreground="White" FontWeight="Bold"/>
                            </Grid>
                        </StackPanel>
                    </TabItem.Header>
                    <TextBlock Text="Citizen Three Schedule" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="50" FontWeight="Bold"/>
                </TabItem>

                <TabItem Visibility="Hidden" Tag="C04">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Citizen 4" VerticalAlignment="Center"/>
                            <Grid Margin="10,1,0,1">
                                <Rectangle Fill="DarkOrange" RadiusX="10" RadiusY="10"/>
                                <TextBlock Text="L32" Padding="10,1" Foreground="White" FontWeight="Bold"/>
                            </Grid>
                        </StackPanel>
                    </TabItem.Header>
                    <TextBlock Text="Citizen Four Schedule" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="50" FontWeight="Bold"/>
                </TabItem>

            </TabControl>
        </Grid>
    </Grid>
</UserControl>

<!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Unallocated" Grid.Column="0" Grid.Row="0" Style="{StaticResource headerTextStyle}"/>
        <ListBox Grid.Column="0" Grid.Row="1" Grid.RowSpan="4" MinWidth="250" Margin="0,0,25,0" HorizontalContentAlignment="Stretch" ItemsSource="{Binding unallocated, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <uc:DisplayAwaitingScheduling order="{Binding}" MouseDown="DisplayLMOScheduling_MouseClick_awaiting"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Text="Citizen One" Grid.Column="1" Grid.Row="0" Style="{StaticResource headerTextStyle}"/>
        <ListBox Grid.Column="2" Grid.Row="1" HorizontalContentAlignment="Stretch" ItemsSource="{Binding C1items, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            -->
<!--<Border BorderBrush="DodgerBlue" BorderThickness="5"/>-->
<!--
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <uc:DisplayLMOScheduling order="{Binding}" MouseDown="DisplayLMOScheduling_MouseClick"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Text="Citizen Two" Grid.Column="1" Grid.Row="2" Style="{StaticResource headerTextStyle}"/>
        <ListBox Grid.Column="4" Grid.Row="3" HorizontalContentAlignment="Stretch" ItemsSource="{Binding C2items, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <uc:DisplayLMOScheduling order="{Binding}" MouseDown="DisplayLMOScheduling_MouseClick"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
    

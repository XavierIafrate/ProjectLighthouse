<UserControl
    x:Class="ProjectLighthouse.View.UserControls.DisplayDeliveryNote"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="200"
    MinHeight="50"
    d:Background="White"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel VerticalAlignment="Center">
            <TextBlock
                x:Name="MOTextBlock"
                FontSize="22"
                FontWeight="Bold"
                Foreground="{StaticResource OnBackground}"
                Text="{Binding Name, FallbackValue='M00001'}" />
            <TextBlock
                x:Name="PORTextBlock"
                FontSize="12"
                Foreground="{StaticResource Blue}"
                Text="{Binding DeliveryDate, FallbackValue='01/01/1970', StringFormat='{}Delivered {0:dd/MM/yy HH:mm}'}" />
        </StackPanel>
    </Grid>
</UserControl>

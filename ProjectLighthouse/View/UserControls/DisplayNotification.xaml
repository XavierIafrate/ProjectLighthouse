<UserControl
    x:Class="ProjectLighthouse.View.UserControls.DisplayNotification"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="236.143">
    <Grid>
        <Border
            Margin="5"
            Background="{StaticResource Background}"
            BorderBrush="{StaticResource DisabledElement}"
            BorderThickness="2"
            CornerRadius="8">
            <StackPanel Margin="10,5">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="SeenIndicator" Background="{StaticResource Blue}" Height="10" Width="10" CornerRadius="5" Margin="0,5,5,2"/>
                        <TextBlock
                        VerticalAlignment="Center"
                        FontSize="18"
                        FontWeight="Bold"
                        Text="{Binding Header}" d:Text="New Request" />
                    </StackPanel>
                    <Button
                        Margin="0"
                        HorizontalAlignment="Right"
                        Content="view"
                        Click="Button_Click"
                        Style="{StaticResource ModernGreenButtonTemplate}" />


                </Grid>

                <TextBlock Text="{Binding Body}" d:Text="Lorem ipsum dolor sit amet" TextWrapping="Wrap" />

                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                    <TextBlock
                        FontStyle="Italic"
                        Foreground="{StaticResource Blue}"
                        Text="{Binding Origin}" d:Text="randy" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontStyle="Italic"
                        Foreground="{StaticResource Blue}"
                        Text="{Binding TimeStamp, StringFormat='{} - {0:dd/MM HH:mm}'}" d:Text=" - 01/01 00:00" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

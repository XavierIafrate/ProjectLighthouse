<UserControl x:Class="ProjectLighthouse.View.UserControls.DisplayLMOScheduling"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectLighthouse.View.UserControls"
             xmlns:converters ="clr-namespace:ProjectLighthouse.ViewModel.ValueConverters"
             mc:Ignorable="d"
             d:Background="White">

    <UserControl.Resources>
        <converters:intToDays x:Key="inttoday"/>
        <converters:StartDateToEndDate x:Key="startToFinish"/>
        <converters:orderItemToRequirementText x:Key="requirementText"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Margin="0,5" x:Name="bg" Background="{StaticResource materialError}" CornerRadius="5,0,0,5"/>


        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" MinWidth="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Margin="20,10">
                <TextBlock Text="{Binding Order.Name, FallbackValue='M00001'}" FontSize="25" Foreground="{StaticResource materialOnBackground}" FontWeight="Bold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle x:Name="statusBadgeText" Fill="{StaticResource materialError}" RadiusX="12" RadiusY="12" Height="24"/>
                    <TextBlock Text="{Binding Order.Status, FallbackValue='Ready'}" 
                               Foreground="{StaticResource materialOnPrimary}" 
                               FontSize="14"
                               Padding="10,0,10,2"
                               VerticalAlignment="Center"/>
                </Grid>
                <Button Command="{Binding UpdateCommand}" 
                        CommandParameter="{Binding Order}"
                        Content="edit" 
                        x:Name="editButton"
                        Style="{StaticResource SubtleButtonTemplate}"
                        HorizontalAlignment="Left"
                        Margin="0,5,0,0"/>


            </StackPanel>

            <Grid Grid.Column="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Start" HorizontalAlignment="Right" Margin="0,0,15,0" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Order.StartDate, StringFormat='{}{0:ddd, dd MMM}'}" Grid.Column="1"/>

                <TextBlock Text="Finish" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,15,0" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Order, Converter='{StaticResource startToFinish}', StringFormat='{}{0:ddd, dd MMM}'}" Grid.Column="1" Grid.Row="1"/>

                <TextBlock Text="Runtime" Grid.Row="2" Margin="0,0,15,0" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Order.TimeToComplete, Converter='{StaticResource inttoday}', StringFormat='{}{0} days'}" Grid.Row="2" Grid.Column="1"/>

                <TextBlock Text="Bar ID" Grid.Row="3" Margin="0,0,15,0" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Order.BarID}" Grid.Row="3" Grid.Column="1"/>

                <TextBlock Text="# Bars" Grid.Row="4" Margin="0,0,15,0" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Order.NumberOfBars}" Grid.Row="4" Grid.Column="1"/>

            </Grid>
            
            <StackPanel Grid.Column="2" Margin="0,10,20,10">
                <TextBlock Text="Order Items" Margin="5" FontWeight="SemiBold" FontSize="15"/>

                <ItemsControl ItemsSource="{Binding OrderItems}" Grid.IsSharedSizeScope="True" HorizontalContentAlignment="Stretch">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" MinWidth="100" SharedSizeGroup="a"/>
                                    <ColumnDefinition Width="*" MinWidth="120" SharedSizeGroup="b" />
                                    <ColumnDefinition Width="auto" MinWidth="150" SharedSizeGroup="c" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Path=ProductName}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Path=TargetQuantity, StringFormat='{}{0:#,##0} pcs'}" Grid.Column="1"  HorizontalAlignment="Center" Padding="10,0"/>
                                <TextBlock Text="{Binding Converter='{StaticResource requirementText}'}" Grid.Column="2" FontStyle="Italic"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

<UserControl
    x:Class="ProjectLighthouse.View.UserControls.DisplayLatheManufactureOrder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="200"
    MinHeight="50"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Rectangle x:Name="OldInfo" Fill="{StaticResource materialError}" />
        <StackPanel
            Grid.Column="1"
            Margin="10,5,5,5"
            VerticalAlignment="Center">
            <TextBlock
                x:Name="MOTextBlock"
                FontSize="22"
                FontWeight="Bold"
                Foreground="{StaticResource materialOnBackground}"
                Text="{Binding Name, FallbackValue='M00001'}" />
            <TextBlock
                x:Name="PORTextBlock"
                FontSize="12"
                Foreground="{StaticResource materialOnBackground}"
                Text="{Binding POReference, FallbackValue='POR012345'}" />
        </StackPanel>
        <Grid
            Grid.Column="3"
            Margin="10,0"
            VerticalAlignment="Center">
            <Rectangle
                x:Name="badgeBackground"
                Fill="{StaticResource materialError}"
                RadiusX="5"
                RadiusY="5" />
            <TextBlock
                x:Name="BadgeText"
                Padding="15,0,15,2"
                FontSize="14"
                Foreground="{StaticResource materialOnPrimary}"
                Text="Ready" />
        </Grid>
    </Grid>
</UserControl>

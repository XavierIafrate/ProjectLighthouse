<UserControl x:Class="ProjectLighthouse.View.UserControls.DisplaySubAssembly"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectLighthouse.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource materialOnBackground}" Margin="0,5">
        <StackPanel Margin="10">
            <TextBlock Text="{Binding product.ProductNumber}"
                       Foreground="{StaticResource materialBackground}"
                       FontSize="16"/>
            <Separator Background="WHite" Margin="0,7"/>
            <TextBlock Text="Bill of Materials"
                       Margin="0,5"
                       Foreground="{StaticResource materialBackground}"
                       FontSize="14"/>
            <ItemsControl ItemsSource="{Binding materials.Items}" Margin="20,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding ComponentItem}" Foreground="White"/>
                            <TextBlock Text="{Binding Quantity}" Foreground="White" Grid.Column="2"/>
                            <TextBlock Text="{Binding Unit}" Foreground="White" Grid.Column="3"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="Routings"
                       Margin="0,10,0,5"
                       Foreground="{StaticResource materialBackground}"
                       FontSize="14"/>
            <ItemsControl ItemsSource="{Binding routings.RoutingItems}" Margin="20,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Description}" Foreground="White"/>
                            <TextBlock Text="{Binding Workstation}" Foreground="White" Grid.Column="2"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>

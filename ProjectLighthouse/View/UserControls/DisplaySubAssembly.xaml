<UserControl
    x:Class="ProjectLighthouse.View.UserControls.DisplaySubAssembly"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="0,5">
        <StackPanel Margin="10">
            <TextBlock Style="{StaticResource subtitle}" Text="{Binding product.ProductNumber}" />
            <Separator Margin="0,7" Background="{StaticResource materialOnBackground}" />
            <TextBlock Style="{StaticResource statement}" Text="Bill of Materials" />
            <ItemsControl Margin="20,0" ItemsSource="{Binding materials.Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Foreground="{StaticResource materialOnBackground}" Text="{Binding ComponentItem}" />
                            <TextBlock
                                Grid.Column="2"
                                Foreground="{StaticResource materialOnBackground}"
                                Text="{Binding Quantity}" />
                            <TextBlock
                                Grid.Column="3"
                                Foreground="{StaticResource materialOnBackground}"
                                Text="{Binding Units, StringFormat='{} {0}'}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Style="{StaticResource statement}" Text="Routings" />
            <ItemsControl Margin="20,0" ItemsSource="{Binding routings.RoutingItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Foreground="{StaticResource materialOnBackground}" Text="{Binding Description}" />
                            <TextBlock
                                Grid.Column="2"
                                Foreground="{StaticResource materialOnBackground}"
                                Text="{Binding Workstation}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>

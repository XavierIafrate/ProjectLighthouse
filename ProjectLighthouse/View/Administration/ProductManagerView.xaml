<UserControl
    x:Class="ProjectLighthouse.View.Administration.ProductManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View.Administration"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:Background="White"
    d:DesignHeight="900"
    d:DesignWidth="1350"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    x:Name="searchBox"
                    IsEnabled="False"
                    Style="{StaticResource SearchBox}"
                    Text="{Binding SearchString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button
                    x:Name="ClearButton"
                    Grid.Column="1"
                    Click="ClearButton_Click"
                    IsEnabled="False"
                    Style="{StaticResource ClearButton}" />
            </Grid>
            <ListView
                Grid.Row="1"
                Margin="5"
                HorizontalContentAlignment="Stretch"
                d:ItemsSource="{d:SampleData ItemCount=15}"
                ItemsSource="{Binding Products}"
                SelectedValue="{Binding SelectedProduct, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Margin="7,5"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Grid Grid.Row="2">
                <Button
                    HorizontalAlignment="Left"
                    Style="{StaticResource Action_New_Button}"
                    Tag="New Product" />
            </Grid>
        </Grid>
        <!--<TextBlock
            Grid.Column="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Style="{StaticResource GhostText}"
            Text="None found"
            Visibility="{Binding NoneFoundVis}" />-->

        <!--  Visibility="{Binding BarStockVis}"  -->

        <ScrollViewer Grid.Column="1" ScrollChanged="ScrollViewer_ScrollChanged">
            <StackPanel MaxWidth="700">
                <Grid>
                    <StackPanel>
                        <TextBlock
                            d:Text="PRODUCT_NAME"
                            Style="{StaticResource title}"
                            Text="{Binding SelectedProduct.Name}" />
                        <TextBlock
                            d:Text="PRODUCT_NAME"
                            Style="{StaticResource subtitle}"
                            Text="{Binding SelectedProduct.Description}" />
                        <Button
                            Margin="0,10,0,0"
                            HorizontalAlignment="Left"
                            Style="{StaticResource Action_Button}"
                            Tag="View Web Page" />
                    </StackPanel>

                    <Border
                        HorizontalAlignment="Right"
                        Width="120"
                        Height="120"
                        Margin="5"
                        Background="{StaticResource Surface}"
                        CornerRadius="5">
                        <Grid>
                            <Path
                                Height="50"
                                Margin="0,0,2,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="M1,10V2L10,5V7L1,10M8,10V8H13V4H8V3L12,1L16,3V4H15V8H16V10H14.74L8.44,13.64L9,10H8M7,23L7.04,22.76L16.15,17.5L16.67,20.88L13,23H7M8.05,16.17L15.31,12L15.83,15.37L7.43,20.22L8.05,16.17Z"
                                Fill="{StaticResource OnSurface}"
                                Opacity="0.3"
                                Stretch="Uniform" />
                            <Image
                                Margin="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                RenderOptions.BitmapScalingMode="Fant"
                                Source="{Binding SelectedProduct.LocalRenderPath}" />
                        </Grid>

                    </Border>
                </Grid>

                <TextBlock Style="{StaticResource section}" Text="{Binding FilteredProductGroups.Count, StringFormat='{}Archetypes [{0}]'}" />
                <ListView
                    d:ItemsSource="{d:SampleData ItemCount=5}"
                    ItemsSource="{Binding FilteredProductGroups}"
                    SelectedValue="{Binding SelectedProductGroup, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                Margin="7,5"
                                FontWeight="SemiBold"
                                Text="{Binding Name}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Style="{StaticResource section}" Text="{Binding FilteredTurnedProducts.Count, StringFormat='{}Products in Archetype [{0}]'}" />
                <ItemsControl d:ItemsSource="{d:SampleData ItemCount=10}" ItemsSource="{Binding FilteredTurnedProducts}" />

                <Grid Height="200" />
            </StackPanel>
        </ScrollViewer>
        <Border
            x:Name="grad"
            Grid.RowSpan="2"
            Grid.Column="1"
            Height="20"
            VerticalAlignment="Top"
            CornerRadius="20,0,0,0"
            IsHitTestVisible="False">
            <Border.Background>
                <LinearGradientBrush Opacity="0.08" StartPoint="0,1" EndPoint="0,0">
                    <GradientStop Offset="0" Color="Transparent" />
                    <GradientStop Offset="1" Color="Gray" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>
    </Grid>
</UserControl>

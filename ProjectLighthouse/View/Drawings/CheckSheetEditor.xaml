<Window
    x:Class="ProjectLighthouse.View.Drawings.CheckSheetEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectLighthouse.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:quality="clr-namespace:ProjectLighthouse.Model.Quality"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="CheckSheetEditor"
    Width="1170"
    Height="672"
    MaxWidth="2000"
    MaxHeight="2000"
    Background="#333"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <!--<ColumnDefinition Width="5" />-->
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.ColumnSpan="2"
            Margin="20"
            VerticalAlignment="Center"
            FontSize="30"
            FontWeight="SemiBold"
            Foreground="{StaticResource BlueLight}"
            Text="Check Sheet Constructor" />
        <StackPanel
            Grid.Column="3"
            HorizontalAlignment="Right"
            VerticalAlignment="Center">
            <Button
                x:Name="TestButton"
                Click="TestButton_Click"
                Content="Check Contents" />
            <Button
                x:Name="ToggleNavButton"
                Click="ToggleNavButton_Click"
                Content="Toggle PDF" />
        </StackPanel>
        <DataGrid
            x:Name="DimensionGrid"
            Grid.Row="1"
            d:ItemsSource="{d:SampleData ItemCount=10}"
            CanUserSortColumns="False" />
        <wv2:WebView2
            Name="webView"
            Grid.Row="1"
            Grid.Column="1"
            Source="file:///C:/Users/xavie/Documents/checksheet.pdf" />
        <TextBox
            x:Name="ConsoleOutput"
            Grid.Row="1"
            Grid.Column="2"
            Background="Purple"
            Foreground="White"
            IsEnabled="False"
            Text="" />
    </Grid>
</Window>

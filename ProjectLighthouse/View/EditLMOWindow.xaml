<Window x:Class="ProjectLighthouse.View.EditLMOWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectLighthouse.View"
        xmlns:uc="clr-namespace:ProjectLighthouse.View.UserControls"    
        mc:Ignorable="d"
        Title="Edit Order"
        SizeToContent="Height"
        Width="600">
    
    
    <Grid Margin="25">
        <!--Header Area-->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="Transparent" MouseDown="Rectangle_MouseDown"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="M00001" x:Name="nameText" FontSize="50" FontWeight="Bold"/>
            <Button Content="Close" Style="{StaticResource CancelButtonTemplate}" Grid.Column="2" Click="closeButton_Click"/>
            <Separator Grid.Row="1" Grid.ColumnSpan="3" Margin="0,0,0,15"/>
        </Grid>
        <!--Main controls-->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Administration" Style="{StaticResource subtitleTextStyle}"/>
            <StackPanel x:Name="AdminControls">
                <TextBlock Text="Purchase Order Reference" Style="{StaticResource bodyTextStyle}"/>
                <TextBox x:Name="PORef"/>
                
                <TextBlock Text="Allocated Setter" Style="{StaticResource bodyTextStyle}"/>
                <ComboBox x:Name="setters" Margin="0,0,0,10"/>

                <CheckBox Content="Has Program" x:Name="program" Click="program_Click"/>
                <CheckBox Content="Ready to Set" x:Name="ready" Click="ready_Click"/>

                <CheckBox Content="Urgent" x:Name="urgent" Margin="0,10"/>
                
                <TextBlock Text="Set Status" Style="{StaticResource bodyTextStyle}"/>
                <ComboBox>
                    <ComboBoxItem Content="Ready"/>
                    <ComboBoxItem Content="Problem"/>
                    <ComboBoxItem Content="Running"/>
                    <ComboBoxItem Content="Complete"/>
                </ComboBox>
                <Button Style="{StaticResource CancelButtonTemplate}" 
                        Content="Cancel Order"
                        x:Name="cancelOrderButton"
                        Click="CancelOrderButton_Click"/>
            </StackPanel>
            
            <!--Items List-->
            <TextBlock Text="Items" Style="{StaticResource subtitleTextStyle}"/>
            <ListBox x:Name="ItemsListBox" HorizontalContentAlignment="Stretch" MaxHeight="300" ScrollViewer.CanContentScroll="False" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <uc:DisplayLMOItems LatheManufactureOrderItem="{Binding}" MouseDoubleClick="DisplayLMOItems_MouseDoubleClick"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Notes" Style="{StaticResource subtitleTextStyle}"/>
            <Rectangle RadiusX="10" RadiusY="10" Stroke="{StaticResource materialPrimary}" StrokeThickness="2" Grid.Row="1"/>
            <RichTextBox MinHeight="100" BorderThickness="0" Margin="10" Background="Transparent" x:Name="notes" Grid.Row="1">
            </RichTextBox>
        </Grid>
        <Button x:Name="saveButton" 
                    Style="{StaticResource ButtonTemplate}" 
                    Content="Save and Close" Width="150" 
                    HorizontalAlignment="Right" 
                    Click="saveButton_Click"
                    Grid.Row="3"/>
    </Grid>
</Window>

<UserControl x:Class="ProjectLighthouse.View.CalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProjectLighthouse.ViewModel"
             xmlns:local="clr-namespace:ProjectLighthouse.View"
             xmlns:uc="clr-namespace:ProjectLighthouse.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="724" d:DesignWidth="1324"
             d:Background="White">
    <UserControl.Resources>
        <vm:CalibrationViewModel x:Key="vm"/>
    </UserControl.Resources>

    <Grid DataContext="{StaticResource vm}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{Binding Equipment}" SelectedItem="{Binding SelectedEquipment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <uc:DisplayCalibratedEquipment Equipment="{Binding}" Margin="10,5"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="{Binding SelectedEquipment.Name}" Style="{StaticResource title}"/>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding SelectedEquipment.Make}" Style="{StaticResource subtitle}"/>
                <TextBlock Text="{Binding SelectedEquipment.Model, StringFormat='{} {0}'}" Style="{StaticResource subtitle}"/>
                <TextBlock Text="{Binding SelectedEquipment.SerialNumber, StringFormat='{}, S/N: {0}'}" Style="{StaticResource subtitle}"/>
            </StackPanel>

            <TextBlock Text="{Binding SelectedEquipment.LastCalibrated, StringFormat='{}Last Calibration: {0:dd/MM/yyyy}'}" Style="{StaticResource subtitle}"/>
            <TextBlock Text="{Binding SelectedEquipment.NextDue, StringFormat='{}Next Calibration Due: {0:dd/MM/yyyy}'}" Style="{StaticResource subtitle}"/>
            <TextBlock Text="{Binding SelectedEquipment.LastVisualCheck, StringFormat='{}Last Visual Check: {0:dd/MM/yyyy}'}" Style="{StaticResource subtitle}"/>
            <TextBlock Text="{Binding SelectedEquipment.CalibrationIntervalMonths, StringFormat='{}Interval: {0:0}mo'}" Style="{StaticResource subtitle}"/>
            <TextBox Style="{StaticResource DefaultTextBox}" Tag="Interval"/>
            <TextBlock Text="Certificates" Style="{StaticResource subsubtitle}"/>
            <ListView ItemsSource="{Binding Certificates}"/>
        </StackPanel>
    </Grid>
</UserControl>
